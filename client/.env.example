# API Configuration
# In development with Vite proxy, use relative path '/api'
# In production, the app is served by Express, so '/api' works
# If deploying client separately, set to your API server URL
VITE_API_BASE_URL=/api
