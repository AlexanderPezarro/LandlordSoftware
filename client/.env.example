# API Configuration
# In development with Vite proxy, use relative path '/api' (default)
# If deploying client separately from server, set to your API server URL
# Examples:
#   Same server: VITE_API_BASE_URL=/api
#   Separate deployment: VITE_API_BASE_URL=https://api.yourdomain.com/api
VITE_API_BASE_URL=/api
