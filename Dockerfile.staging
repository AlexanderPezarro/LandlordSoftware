# syntax = docker/dockerfile:1

# Staging Dockerfile - extends base image

FROM registry.fly.io/landlordsoftware-base:latest

# Copy production entrypoint (staging uses same entrypoint as production)
COPY docker-entrypoint.js /app/docker-entrypoint.js
RUN chmod +x /app/docker-entrypoint.js

ENTRYPOINT ["/app/docker-entrypoint.js"]
CMD ["npm", "run", "start"]
